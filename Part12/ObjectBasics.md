# objects 

- Object is a collection of data that represented as key-value pairs.
- You can define object types using `type` or `interface`.
- TypeScript checks that the object matches the defined structure.
- Object is a group of related data into one structure.
