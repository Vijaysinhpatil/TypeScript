# 📘 TypeScript Basics – Numbers & Strings

## 🔢 Number Type Annotation
- Used to define variables that store numeric values.
- Supports integers, decimals, negatives, and special values like `NaN` as per annotation type.
- Common operations include arithmetic and mathematical functions like `Math.sqrt()`.

---

## 🧾 String Operations

### ✨ Declaration
- Strings are defined using single, double, or backtick quotes.
- Type annotation ensures variables hold only string values.

### ➕ Concatenation
- Combines two or more strings into one using the `+` operator.

### 📏 Length
- Returns the total number of characters in the string.

### 🔠 Case Conversion
- Converts text to uppercase or lowercase for formatting and comparison.

### ✂️ Substring (Slice)
- Extracts a portion of the string between specified start and end positions.

### ⚖️ String Comparison
- `localeCompare()` compares two strings based on language rules.
- Returns negative, zero, or positive depending on order.

### 🧩 Equality Check
- Uses `===` to check if two strings are exactly the same (case-sensitive).

### 🧱 Template Literals
- Uses backticks to embed variables and expressions directly into strings.

---

💬 **Tip:**  
> “Understand small concepts deeply — that’s how you master programming.” 🚀
