#  map() :

- Used to transform each element of an array into a new value.

- Returns a new array of the same length with the transformed elements.

- Does not modify the original array.

# filter() :

- Used to select elements from an array based on a given condition.

- Returns a new array containing only the elements that satisfy the condition.

- Does not modify the original array.


