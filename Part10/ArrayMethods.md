# Array Methods

- It allows u to perform common operations on arrays.

# Push() :

- It is used to add an elements at the end of the array.
- If we push the elemets by stroing it to the another variable then if we print that pushed elemets stroed variable then it returns the lotal length of the origional array.

# Pop() :

- It is used to remove the last element of the array.

# shift() :

- It is used to remove the first elemet of the array.

# Iterating over an array :

- Going through each item one by one in a list or group.
- There are several ways like for , for...of , forEach() 

# for...in loop : 

- It is a way to access the indices of an array one by one.

# for...of loop : 

- It is a way to accees the index value one by one.

# forEach() :

- It takes a callback() function as an argument and then iterate an array one-by-one.
- callback() function returns the index and current index value.

```
arrayName.forEach((str : string) => (log(str)))

```